import javax.swing.*;
import java.awt.*;

public class Slots extends JPanel {
    public Slots() {
        this.setLayout(null);
        this.setBackground(Color.BLACK);
        this.setVisible(true);
    }
}
